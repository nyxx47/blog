import { withKnobs, text, select } from "@storybook/addon-knobs";
import { Meta, Story,Props, Preview } from '@storybook/addon-docs/blocks';

import ArticleText from './articleText.jsx'
import Img from '../../../../static/illustrations/issue-flow.svg'

import Knobs from './articleText.knobs.json'
const {items} = Knobs.data

<Meta title="Organisms|Articles|ArticleText" component={ArticleText}/>

## ArticleText
ArticleText is small and adaptive tag for adding context to content.

<Preview withToolbar>
    <Story name="ArticleText">
        <ArticleText items={items} outline/>
    </Story>
</Preview>

<Props of={ArticleText} />

## ArticleText

Import your `ArticleText component` in your file

```jsx
import { ArticleText } from "../your_path/components/organisms";

```

```jsx
<ArticleText/>
```

