import { withKnobs, text, select } from "@storybook/addon-knobs";
import { Meta, Story,Props, Preview } from '@storybook/addon-docs/blocks';

import ButtonIcon from './buttonIcon.jsx'

<Meta title="Atoms|Buttons|ButtonIcon" component={ButtonIcon}/>

## ButtonIcon
ButtonIcon component allows to display various formats of graphics. Use it to show product images, table list thumbnails and similar.

<Preview withToolbar>
    <Story name="ButtonIcon">
        <ButtonIcon name="arrowBack" variant="secondary" />
    </Story>
</Preview>

<Props of={ButtonIcon} />

## USAGE

Import your `ButtonIcon component` in your file

```jsx
import { ButtonIcon } from "../your_path/components";

```

```jsx
    <ButtonIcon/>
```
