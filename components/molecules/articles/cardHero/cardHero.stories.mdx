import { withKnobs, text, select } from "@storybook/addon-knobs";
import { Meta, Story,Props, Preview } from '@storybook/addon-docs/blocks';

import CardHero from './cardHero.jsx'
import Knobs from './cardHero.knobs.json'
const {items} = Knobs

<Meta title="Molecules|Articles|CardHero" component={CardHero}/>

## CardHero
CardHero is small and adaptive tag for adding context to content.

<Preview withToolbar>
    <Story name="CardHero">
        <CardHero item={items[0]}/>
    </Story>
</Preview>

## USAGE 

Import your `CardHero component` in your file
In this case, I used knobs file like this.

```json
{
    "items": [
        {
            "type": "large",
            "direction": "rows",
            "hero": {
                "image": "https://increment.com/art/12/case-studies-in-reachitecting/cover-1000-570f4642.jpeg",
                "class": "semi-half"
            },
            "content": {
                "title": "Case studies in rearchitecting",
                "subtitle": "How Buffer, ThoughtWorks, N26, and Zapier have shifted their software to respond to new contexts and met new needs."
            }
        }
    ]
}

```

```jsx
import { CardHero } from "../your_path/components/molecules";

```

```jsx
<CardHero item={items[0]}/>
```

